# my-living-api-test — API Documentation

> Auto-generated by **Living API Docs** on 2026-04-22 04:50 UTC
> Framework: **Fastapi**
> Source: [https://github.com/anushree196/my-living-api-test.git](https://github.com/anushree196/my-living-api-test.git)

---

## Table of Contents

- [GET /](#get-)
- [GET /hello/{name}](#get-helloname)
- [GET /users](#get-users)
- [POST /users](#post-users)
- [DELETE /users/{user_id}](#delete-usersuser-id)

---

# GET /

## Overview
The root endpoint is a simple API entry point that returns a greeting message. It can be used to test the API's availability and responsiveness. This endpoint is useful for developers who want to verify that the API is working correctly before making more complex requests.

## Real-World Use Case
A developer might call this endpoint to verify that their API client is configured correctly and can reach the server. For example, a user might use this endpoint to test their API connection before making a more complex request.

## Authentication
This is a public endpoint and does not require authentication.

## Parameters

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|

### Query Parameters
| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|

### Request Body
Not applicable.

## Response

### Success Response
**Status:** 200 OK
```json
{
  "message": "Hello World"
}
```

### Error Responses
| Status Code | Meaning | When it occurs |
|-------------|---------|----------------|
| 500 | Server Error | Unexpected failure |

## Code Examples

### Python
```python
import requests
response = requests.get('http://localhost:8000/')
print(response.json())
```

### JavaScript
```javascript
fetch('http://localhost:8000/', {
  method: 'GET',
  headers: {'Content-Type': 'application/json'}
}).then(r => r.json()).then(console.log);
```

## Notes
This endpoint does not have any rate limits or specific error cases beyond standard server errors. It is intended as a simple test point and should not be used for production traffic.

---

# GET /hello/{name}

## Overview
The `/hello/{name}` endpoint is a simple API entry point that returns a personalized greeting message. It can be used to test the API's availability and responsiveness. This endpoint is useful for developers who want to verify that the API is working correctly before making more complex requests. The greeting message is customized based on the provided `name` parameter.

## Real-World Use Case
A developer might call this endpoint to verify that their API client is configured correctly and can reach the server, by passing in their name or a test name to receive a greeting.

## Authentication
This is a public endpoint and does not require authentication.

## Parameters

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| name | str | Yes | The name to be used in the greeting message |

### Query Parameters
| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|

### Request Body
Not applicable.

## Response

### Success Response
**Status:** 200 OK
```json
{
  "message": "Hello John!"
}
```

### Error Responses
| Status Code | Meaning | When it occurs |
|-------------|---------|----------------|
| 400 | Bad Request | Invalid input |
| 500 | Server Error | Unexpected failure |

## Code Examples

### Python
```python
import requests
response = requests.get('http://localhost:8000/hello/John')
print(response.json())
```

### JavaScript
```javascript
fetch('http://localhost:8000/hello/John', {
  method: 'GET',
  headers: {'Content-Type': 'application/json'}
}).then(r => r.json()).then(console.log);
```

## Notes
This endpoint does not have any rate limits or specific error cases beyond standard server errors. It is intended as a simple test point and should not be used for production traffic. The `name` parameter should be a string, and the endpoint will return a greeting message with the provided name.

---

# GET /users

## Overview
The GET /users endpoint retrieves a list of all users from the database. This endpoint can be used to fetch user data for display or processing purposes. It is a simple, read-only operation that does not modify any data.

## Real-World Use Case
A developer might call this endpoint to populate a user management dashboard or to synchronize user data with an external system.

## Authentication
This is a public endpoint and does not require authentication.

## Parameters

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| None      |      |          |             |

### Query Parameters
| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| None      |      |          |         |             |

### Request Body
Not applicable for GET requests.

## Response

### Success Response
**Status:** 200 OK
```json
{
  "users": [
    {"id": 1, "name": "John Doe"},
    {"id": 2, "name": "Jane Doe"}
  ]
}
```

### Error Responses
| Status Code | Meaning | When it occurs |
|-------------|---------|----------------|
| 400 | Bad Request | Invalid input |
| 500 | Server Error | Unexpected failure |

## Code Examples

### Python
```python
import requests
response = requests.get('http://localhost:8000/users')
print(response.json())
```

### JavaScript
```javascript
fetch('http://localhost:8000/users', {
  method: 'GET',
  headers: {'Content-Type': 'application/json'}
}).then(r => r.json()).then(console.log);
```

## Notes
This endpoint returns all users in the database. Be aware that this may be a large dataset for very large user bases. Also, note that the response format may vary depending on the actual data stored in the `users_db` variable.

---

# POST /users

## Overview
The POST /users endpoint creates a new user in the database. This endpoint can be used to add a new user to the system, and it returns the newly created user's details. It is a simple, write-only operation that modifies the user database.

## Real-World Use Case
A developer might call this endpoint to create a new user account when a user signs up for a service or to add a new user to a user management system.

## Authentication
This is a public endpoint and does not require authentication.

## Parameters

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| None      |      |          |             |

### Query Parameters
| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| None      |      |          |         |             |

### Request Body
Not applicable for this endpoint, as it does not require any input data to create a new user.

## Response

### Success Response
**Status:** 200 OK
```json
{
  "id": 1,
  "name": "New User"
}
```

### Error Responses
| Status Code | Meaning | When it occurs |
|-------------|---------|----------------|
| 400 | Bad Request | Invalid input |
| 500 | Server Error | Unexpected failure |

## Code Examples

### Python
```python
import requests
response = requests.post('http://localhost:8000/users')
print(response.json())
```

### JavaScript
```javascript
fetch('http://localhost:8000/users', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'}
}).then(r => r.json()).then(console.log);
```

## Notes
This endpoint creates a new user with a default name "New User" and an ID that is one more than the current number of users in the database. The response format may vary depending on the actual data stored in the `users_db` variable. Be aware that this endpoint does not handle duplicate user creation or validation of user data.

---

# DELETE /users/{user_id}

## Overview
The DELETE /users/{user_id} endpoint is used to delete a user from the database by their unique identifier. This endpoint is useful when you need to remove a user's data from your system. It returns a success message if the user is found and deleted.

## Real-World Use Case
A developer might call this endpoint when a user requests to delete their account, or when an administrator needs to remove a user from the system.

## Authentication
This endpoint is a public endpoint and does not require authentication.

## Parameters

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| user_id | int | Yes | The unique identifier of the user to be deleted |

### Query Parameters
| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| None |  |  |  | No query parameters are accepted |

### Request Body
Not applicable

## Response

### Success Response
**Status:** 200 OK
```json
{
  "message": "User deleted"
}
```

### Error Responses
| Status Code | Meaning | When it occurs |
|-------------|---------|----------------|
| 404 | Not Found | The user with the specified ID does not exist |

## Code Examples

### Python
```python
import requests
user_id = 1
response = requests.delete(f'http://localhost:8000/users/{user_id}')
print(response.json())
```

### JavaScript
```javascript
fetch('http://localhost:8000/users/1', {
  method: 'DELETE',
  headers: {'Content-Type': 'application/json'}
}).then(r => r.json()).then(console.log);
```

## Notes
Be careful when using this endpoint, as it permanently deletes a user's data. Make sure to handle errors and exceptions properly to avoid unexpected behavior. Also, note that this endpoint does not handle cases where the user ID is not an integer or is not provided.

---


*Generated by Living API Docs | 2026-04-22*